# Production Environment Variables
# Bu dosyayı .gitignore'a ekleyin!

# Database (DigitalOcean otomatik doldurur)
DATABASE_URL=postgresql://user:password@host:5432/database

# JWT Secret (değiştirin - güçlü random string kullanın!)
JWT_SECRET_KEY=change-this-to-a-very-long-random-string-in-production

# Environment
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=info

# Server
PORT=8080
HOST=0.0.0.0

# CORS (production domain'inizi ekleyin)
ALLOWED_ORIGINS=https://your-app.ondigitalocean.app,https://your-custom-domain.com

# Rate Limiting (opsiyonel)
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=60

# Session
SESSION_EXPIRE_HOURS=24

# WebSocket
WS_HEARTBEAT_INTERVAL=30
WS_MAX_CONNECTIONS_PER_USER=5

# File Upload Limits
MAX_FILE_SIZE_MB=10
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf

# Email (opsiyonel - kullanıcı onay maili için)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@yourdomain.com

# Monitoring (opsiyonel)
SENTRY_DSN=your-sentry-dsn-here

# Shopify & Trendyol
# Bu ayarlar artık kullanıcı bazlı database'de
# Kullanıcılar Settings ekranından girecek
